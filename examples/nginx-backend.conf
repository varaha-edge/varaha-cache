server {
    listen 8132;
    server_name localhost;

    location / {
        add_header X-Backend "nginx-origin";
        return 200 "Hello from backend\n";
    }

    location /health {
        return 200 "OK\n";
    }
}
